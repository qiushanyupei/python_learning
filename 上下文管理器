可以用于文件打开关闭、数据库连接关闭的代码简化，文件和数据库连接本质都是封装好的类，有后面说的with类需要的内容
本来如果不用它，需要写更多代码来防止异常情况
由__enter__和__exit__函数相当于构造函数和析构函数，定义了进入和退出with块时做的操作
__enter__返回的值会绑定在as后的变量上，例with open(“test.txt”) as f
__exit__函数的参数顺序是(exc_type, exc_value, traceback)
__exit__方法返回True时，with语句内的异常会被抑制（会报错，但是不会中断程序，后面的语句依旧执行）
